spring.main.web-application-type=servlet
server.port=${PORT:8080}

app.mail.from=booking@royalchappy.com
app.mail.admin=staff@royalchappy.com
app.base-url=https://royalchappy.com
resend.api.key=${RESEND_API_KEY}

# Square credentials
square.application.id=sq0idp-SxNqv-moqYZxaHtfYTwssg
square.access.token=${SQUARE_ACCESS_TOKEN}
square.location.id=${SQUARE_LOCATION_ID}

# Admin Access
admin.api.key=${ADMIN_API_KEY}

# DATABASE
spring.datasource.url=${JDBC_DATABASE_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.driver-class-name=org.postgresql.Driver
server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.secure=true
server.servlet.session.cookie.http-only=true

#Square Category
square.tee-time.variation-id=4LL4XWGYYJPVS6OZSKDFGR67